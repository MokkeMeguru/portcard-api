-rw-r--r--
 110100100
 644
 
 
 vm instance を作成
- ここで NGINXサーバなど立ててHTTP x 動的IPアドレスのケースでちゃんと動くか検証しておく
 
 VPC ネットワークを作成
 ⇒ファイアウォールルールで ssh は足しておく (接続できなくなる)
 サブネットを作成
=> ipaddr range　10.146.0.0/20 (default を参考にする)
vm instance の設定から、ネットワークインターフェースを、作成したVPCネットワークのものに変える

GCE インスタンスグループを作る　⇒ 非マネージドインスタンスグループ (vm を先に用意するので)

負荷分散⇒ロードバランサを設定 (ここが クライアント -> https -> http -> サーバ)

https 負荷分散
internat->vm

バックエンドサービスの構成ー＞バックエンドサービスの作成
バックエンドタイプ⇒インスタンスグループ

新しいバックエンド
ポート番号 80

ヘルスチェック (標準設定で新規作成)


フロントエンドの構成
protocol https

証明書 -> google domain などのドメインを貼り付けた物を用意する
(外部へのIPアドレスが確定したら、そのアドレスを google domains から貼り付ける)
つまり、証明書を作るときには subdomain.domain ノカタチにして、
google domains のカスタム リソース レコード　には subdomain, type=A, ttl=5m, data = got ip addr にしなければならない
